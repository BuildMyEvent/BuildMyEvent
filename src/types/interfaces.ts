export interface NavBarInterface {
    title: string,
    url: string
}
export interface Item {
    name: string;
    description: string;
    icon: string;
    color: string;
    time: string;
  }
export interface Event {
    id:string,
    title:string,
    description:string,
    img:string,
}